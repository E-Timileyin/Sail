app:
  name: Sail
  environment: development

deployment:
  container_name: sail-api
  dockerfile: ./Dockerfile
  port: 8080

servers:
  - name: ssh-test
    host: localhost
    port: 2222
    user: root
    password: root
